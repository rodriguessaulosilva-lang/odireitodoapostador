import EmBreve from "@/components/EmBreve";
export const dynamic = "force-dynamic";
export default function Page() {
  return <EmBreve title="Administração" desc="Gestão de usuários (Saulo e Ana Laura), tabela de feriados para o cálculo de prazos e configurações do escritório." />;
}
