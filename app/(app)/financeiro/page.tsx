import EmBreve from "@/components/EmBreve";
export const dynamic = "force-dynamic";
export default function Page() {
  return <EmBreve title="Financeiro" desc="Dashboard de honorários, controle de parcelas, inadimplência, RPVs e projeção de êxito. Já em construção sobre a base de honorários e parcelas do banco." />;
}
