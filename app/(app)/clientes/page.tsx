import EmBreve from "@/components/EmBreve";
export const dynamic = "force-dynamic";
export default function Page() {
  return <EmBreve title="Clientes" desc="CRM com funil de captação (contato → contratado), histórico de interações e vínculo com processos." />;
}
